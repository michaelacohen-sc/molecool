name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  _ numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs

  #- pip:
  #  - codecov

